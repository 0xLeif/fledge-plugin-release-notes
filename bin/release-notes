#!/usr/bin/env bash
set -e
# Find the real path of the binary in case it is symlinked
BIN_PATH="$(python3 -c "import os; print(os.path.realpath('$0'))")"
ROOT="$(cd "$(dirname "$BIN_PATH")/.." && pwd)"

cd "$ROOT"
if [ ! -d "node_modules" ]; then
  bun install --silent
fi
bun run index.ts "$@"
